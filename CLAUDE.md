# TechTok

TikTok-style swipe feed for tech & science news: Expo/React Native Android app + AWS backend (SST v4, Ion architecture) that ingests RSS, condenses articles into cards with Claude, and tracks per-user read state and topic preferences.

The two documents that govern this repo:

- [docs/DESIGN.md](docs/DESIGN.md) — architecture, API, data model. §2 is the **decision log** (D1–D15), §12 the deferred defaults.
- [docs/IMPLEMENTATION_PLAN.md](docs/IMPLEMENTATION_PLAN.md) — 7 phases, each gated by acceptance criteria.

Never re-decide something already in the decision log. If a decision must change, update the log entry with the reason (`/log-decision`), then implement.

## Status

**Phase 0 (walking skeleton) complete and fully verified.** `packages/shared`, `packages/core`, `packages/functions`, `apps/mobile`, and the SST `infra/` are all written; lint + typecheck + test are green (57 tests). Deployed to the `andrey` dev stage on real AWS: DynamoDB table, EventBridge cron, and both API routes are live, the ingest cron has run on its own schedule and populated real posts from all 3 sources, and the Android app has been confirmed on a physical device via Expo Go — cards render with images and swipe correctly. All Phase 0 acceptance criteria are met.

**Phase 1 (users & read state) complete and fully verified.** `Users`/`UserActivity` DynamoDB tables, device-identity middleware (`X-Device-Id`), `POST /v1/reads`, `GET/PUT /v1/me(/topics)`, `GET /v1/history`, and the topic-filtered/read-excluding feed algorithm (DESIGN §5.2) are all written and tested (91 tests total). Mobile adds `zustand` + `@react-native-async-storage/async-storage` for device id, topic-prefs cache, and a persisted read-queue (1.5s page-settle timer, 5s/background flush) wired into `FeedPager`/`Card`, plus new `/settings` and `/history` screens. (Initially built on `react-native-mmkv`, which crashed on launch — Expo Go only supports its bundled native-module set and MMKV v4's Nitro-modules require a custom dev client; swapped to AsyncStorage, which Expo Go does bundle. See the `expo-go-native-module-constraint` memory before adding any other native dep.) Lint + typecheck + test all green, deployed, and confirmed on a physical device via Expo Go: swiped cards never reappear across restarts, two devices have independent read state and topic prefs, deselecting a topic filters the feed, and history paginates newest-first. All Phase 1 acceptance criteria are met.

**Phase 2 (real pipeline) complete and fully verified, including production CI/CD.** `Sources` DynamoDB table seeded with the full ~11-feed preset list (all 3 DESIGN §2 presets); Step Functions `IngestPipeline` (`LoadSources` → `Map(FetchSource, maxConcurrency 4)` → `Summarize`) on an EventBridge Scheduler `rate(30 min)` trigger; SQS `TransformQueue` + DLQ (redrive after 3 receives, `partialResponses` per-message retry); a transform consumer that fetches the article page (10s timeout, 2MB cap, robots.txt-respecting, `TechTokBot` UA), archives raw HTML to S3 (90-day lifecycle), and derives an improved excerpt via `@extractus/article-extractor` (no LLM yet — phase 3); CloudWatch alarms (DLQ depth, SFN execution failures, API 5xx) + a $10 AWS Budget, both wired to one SNS topic; a GitHub Actions `deploy` job (OIDC → AWS, no long-lived keys) that runs `sst deploy --stage production` on merges to `main`. 96 vitest + 14 mobile-jest tests green. Deployed to the `andrey` dev stage and verified live end-to-end: a full pipeline execution fanned out over all 11 sources and succeeded; two sources hit genuine Lambda timeouts and were correctly isolated by the Map's per-item `.catch()` without failing the run; a deliberately-broken feed URL confirmed the primary failure path — `failCount`/`lastStatus` recorded on the `Sources` row, run still succeeded; dedup holds structurally (`postId` is the table's primary key) and was confirmed across repeated executions; a real bug (DynamoDB rejects `status`/`transform` as unaliased reserved words in `UpdateExpression`, silently failing every transform and piling the entire backlog into the DLQ) was caught live, fixed, and confirmed by watching `DlqDepthAlarm` transition `ALARM → OK` after redriving the backlog; a second bug (the Map's per-item `.catch()` replaces state input with `{Error, Cause}` by default, crashing `Summarize` on the first genuine infra failure) was also caught live and fixed. CI/CD: IAM OIDC provider/deploy-role bootstrapped in AWS, `AWS_DEPLOY_ROLE_ARN` GitHub secret added — the trust policy initially used the plain `owner/repo` subject format and needed correcting to GitHub's newer immutable-ID format (`repo:OWNER@ID/REPO@ID:ref:...`, confirmed via CloudTrail `AccessDenied` events) before OIDC auth succeeded. The subsequent `production` `sst deploy` then hit a real AWS account constraint — reserved concurrency 2 on the Transform Lambda (DESIGN §7.2) can't be set because this account's Lambda concurrent-execution quota is stuck at 10, below every function's mandatory 10-unreserved minimum; logged as D16, reserved concurrency is dropped for now on both stages until the quota is raised. With that fix, both the `quality` and `deploy` CI jobs are green and `production` is fully stood up: `IngestPipeline` state machine, all API routes, and the transform pipeline all live under the `techtok-production-*` stack. All Phase 2 acceptance criteria are met.

**Phase 3 (LLM transform) complete.** `core/llm` (Bedrock Converse provider, zod-validated output, one repair-retry, degrade-to-excerpt on any failure), a `Counters` table + atomic daily-cap repo (`eu.anthropic.claude-haiku-4-5-20251001-v1:0`, confirmed the correct ACTIVE EU inference profile ID), `transformArticle` wired to call the LLM under cap, Card DTO/UI show `whyItMatters` + a debug transform badge, and a one-shot backfill script for existing excerpt posts. All Phase 3 acceptance criteria are met.

**Phase 4 (feed quality & polish) — full menu implemented and verified live on the `andrey` dev stage.** All 7 menu items landed: (1) topic onboarding screen on first launch; (2) card design pass — typography/radius/scheme-independent overlay tokens, skeleton loading states, haptic tick on page-settle, optional blurhash plumbing (gated on a future ingest-side field); (3) ranking experiment — `buildFeed` now scores unread candidates by recency-decay(6h half-life) × `Sources.weight` × topic-round-robin interleave, with the publishedAt-sorted candidate list kept separate from the ranked display list so the `nextBefore` pagination cursor never breaks; (4) bookmarks — `UserActivity`'s reserved `bm#` sort-key space plus a dedicated `byBookmarkedAt` GSI, `POST/DELETE/GET /v1/bookmarks`, `isBookmarked` on feed cards, a bookmark icon + `/saved` screen + RN `Share`-based share sheet; (5) image mirroring — a new `Images` S3 bucket + `sst.aws.Router` CloudFront distribution, transform-time mirror step that degrades to the original hotlinked url on any failure; (6) offline prefetch — wifi-gated `Image.prefetch` of the next 3 cards (via `expo-network`'s push-based listener) plus a `@tanstack/query-async-storage-persister`-backed feed cache, closing a real gap where DESIGN.md claimed this already existed; (7) cross-source duplicate collapse (experiment) — token-set-Jaccard title matching within a 48h window, mark-and-filter (never deletes), behind a one-line `DEDUP_ENABLED` toggle. 171 vitest + 26 mobile-jest tests green; every backend piece was exercised live end-to-end (bookmarks CRUD, a real mirrored image loading via CloudFront, a forced `FetchSource` invocation, hand-toggling `duplicateOf` against `GET /v1/feed`). Mobile-side UI (onboarding flow, card redesign, bookmark/share icons, `/saved` screen) still wants a final pass on a physical device via Expo Go. Phase 4's actual exit bar per the implementation plan is subjective — "the feed feels good in daily personal use for a week" — so treat this as feature-complete, not phase-closed, until that week has passed.

**Phase 5 (friends rollout) — code complete, not yet deployed/exercised live.** All 4 required-or-chosen menu items: (1) EAS internal distribution — `apps/mobile/eas.json` (`development`/`preview`/`production` profiles, internal distribution, APK build type) plus `docs/DISTRIBUTION.md` walking through `eas login`/`eas init`/`eas build` and friend-side install; still needs a real `eas init` (writes a real `projectId` into `app.json`) and the production API URL filled into `eas.json` before a build can actually run — both require the maintainer's own EAS/AWS access, so left as placeholders; (2) rate-limit sanity review — API Gateway's account default (5,000 req/s steady / 10,000 burst) is far above friends-scale, so `infra/api.ts` now sets an explicit lower `defaultRouteSettings` ceiling (50 rps / 100 burst) as a sanity check against a client retry-storm bug, not per-device abuse prevention (still explicitly out of scope, DESIGN §5); (3) feedback loop — a "Send feedback" mailto row on `/settings`; (4) daily digest push notifications (the plan's optional item — chosen in, since DESIGN §12 previously listed it as "candidate, not committed") — `Users.pushToken`, `PUT /v1/me/push-token`, `core/notifications/expoPush` (Expo push API, no key needed) + `core/digest/buildDigest` (reuses `buildFeed` for top-5-unread), a `sst.aws.Cron` `DigestCron` (`rate(1 day)`) invoking the new digest Lambda, and a mobile-side opt-in toggle in `/settings` using `expo-notifications`. Important caveat carried over from the `expo-go-native-module-constraint` memory: remote push tokens don't work in plain Expo Go (Expo dropped that in SDK 53) — `enablePushNotifications` silently no-ops there; it's expected to work once distributed via a real EAS build (which Phase 5's own distribution mechanism now is), not via the dev-loop Expo Go testing used for phases 0–4. 174 vitest + 26 mobile-jest tests green. Not yet deployed to any stage or exercised on a device — that, plus the maintainer-only EAS setup steps and getting ≥2 friends installed, is what's left before Phase 5's acceptance criteria can be checked off. **Update 2026-07-24 (D29):** items (3) and (4) were both later removed — the "Send feedback" Settings row and the entire daily digest push feature (infra, API route, DB field, mobile toggle, `expo-notifications` dependency) were fully retired end-to-end; see D29 in the decision log.

**Phase 7 (images & app shell) code complete and verified live on the `dev` stage; only the on-device/APK check remains, for the maintainer.** All 6 menu items: (1) ingest-side image chain (D24) — rss-parser `customFields` for `media:content`/`media:thumbnail` + `content:encoded` declared on `FeedEntry`, `mapEntryToPost`'s fallback chain extended to `enclosure → media:content → media:thumbnail → <img> in content:encoded → <img> in content → <img> in summary`, fixture tests added for Ars Technica, TechCrunch, and Phys.org (real feed samples); (2) transform-side og:image rung (D24) — when a post has no ingest-time `imageUrl`, `transformArticle` takes `article.image` from the already-fetched `@extractus` result and feeds it through the existing `mirrorImage` step, gated by a new `core/pipeline/genericImageDenylist.ts` (unit-tested); (3) one-shot image backfill Lambda (`functions/src/ops/backfillImages.ts` + `core/pipeline/backfillImages.ts`, pattern matching `backfillLlm`) — pages through posts lacking any image but holding an archived raw HTML page (same `byTime` GSI cursor `backfillLlm` uses, not a scan), mines og:image from the S3 archive, mirrors it; a new narrow `PostsRepo.updateMirroredImage` does the single-field write without touching `status`/`transform`; (4) `ImageStub` component — deterministic gradient (seeded by postId, small hand-picked palette) + topic glyph, snapshot-tested (added `react-test-renderer` + `@types/react-test-renderer` as `apps/mobile` devDependencies — this repo's first component-rendering tests, previously all pure-logic); (5) bottom action bar (D25) — `BottomActionBar` replaces the old floating overlay circles: bookmark+share for whichever card `FeedPager` reports as active, saved/history/settings on the right, real layout height so the pager shrinks to fit (required switching `Card`'s container from `useWindowDimensions()` to `width/height: '100%'`, per react-native-pager-view's own documented fix for `flex:1` not sizing a page's children); (6) launch sequence (D25) — a new `LoadingScreen` (the already-branded native splash's blue background + logo + spinner) replaces both the root layout's hydration gate and the feed's own first-load gate, so cold start reads as one continuous screen instead of a black-screen flash in between; the now-fully-superseded `FeedSkeleton` was deleted. Live-verified end-to-end on `dev` (not production — CI-only deploys, per the hard rule): re-seeded `Sources`, ran a real `IngestPipeline` execution across all 11 live feeds (1,402 posts), let the ~1,400-message `TransformQueue` fully drain twice (0 DLQ messages both times). Ingest-time-only image coverage was 4.9%; after transform it reached 93.2% overall (91.1% non-arXiv, past the ≥80% bar) — Ars Technica/Phys.org/Quanta/The Verge hit 100% via the new ingest-chain rungs, and TechCrunch/Hacker News/Hugging Face Blog/ScienceDaily went from 0% at ingest to 100%/50%/98.7%/100% via the transform-time og:image rung. A real bug surfaced live: `@extractus/article-extractor` doesn't return a page's raw `<meta og:image>` value — for arXiv it resolves to a *different* URL/host entirely (`static.arxiv.org/icons/twitter/arxiv-logo-twitter-square.png`, a Twitter-card image), so the first deploy mirrored the arXiv logo onto 322 posts instead of stubbing them; caught via the live audit, fixed by broadening the denylist to the shared `arxiv-logo` filename token, confirmed by clearing and re-transforming all 322 arXiv posts (now correctly 0/322, stub every time). `BackfillImages` was invoked directly twice: once against the fresh dataset (98 candidates, all correctly skipped with logged reasons — broken image URLs, one 5 MB size-cap hit, since transform had already tried and failed the same mirror), once against a deliberately-cleared post to confirm the success path (`mirrored: 1`). 211 vitest + 34 mobile-jest tests green (up from 174+26). Flagged separately, not part of this phase: `nature` (Nature News) shows 0% image coverage because `nature.com` appears to serve a ~3 KB stub page (no `og:image`, ~240 chars of extractable text) to the transform fetch rather than the real article — likely bot detection or a consent wall, unrelated to the image-chain logic itself. What's left: no Android SDK or Xcode in this environment, so the phase's own APK-build-plus-physical-device check is the maintainer's own step, same as every prior phase's on-device pass. **Update 2026-07-24 (D30):** the splash/`LoadingScreen` "blue background + logo" referenced in item (6) was still the unmodified Expo default template icon at the time — D30 replaced it with a real designed identity (violet `#2A1B5C` background, magenta `#FF3D8A` duotone "T" lettermark) across `app.json`, all Android adaptive-icon layers, the favicon, and `LoadingScreen`. **Update 2026-07-24 (D28):** the transform-side og:image rung from item (2) is now gated by a minimum-dimension quality check — `mirrorImage` reports a three-way `'ok' | 'rejected' | 'failed'` outcome (`image-size@2.0.2`, header-only, no bundling issues under SST's esbuild), `transformArticle` cascades ingest-image → og:image on a `'rejected'` result, and `PostsRepo.updateTransform` gained a `clearImageUrl` flag (a real DynamoDB `REMOVE`) for the case where both candidates fail the 600px bar, so a low-quality image can never be served raw. New posts only, no backfill. 294 vitest + 37 mobile-jest tests green. Live-verified on `dev` via direct Lambda invocation: a real undersized (300×200) image correctly cascaded to a real 1920×1080 og:image and mirrored to CloudFront; a second case with no qualifying og:image at all correctly removed `imageUrl` from the DynamoDB row entirely. **Correction, same day:** D30's "implemented" only ever covered `app.json`/source assets/`LoadingScreen` — the committed bare `android/` project (D18) was never regenerated via `expo prebuild` to match, so every real APK build since has kept the original default Expo icon/splash baked in. Confirmed by decoding the actual compiled resources 2026-07-24, once an on-device APK check finally happened (every prior phase had deferred that check to "the maintainer's own step" without ever actually running it). Superseded by D37 (phase 16), which replaces the identity outright and fixes this sync gap for real.

**Phase 8 (localization) code complete and verified live on the `dev` stage; only the on-device Expo Go pass remains, for the maintainer.** All 7 menu items: (1) shared contracts — `Language` (`en`/`ru`/`uk`/`pl`) + per-language `TOPIC_LABELS`/`getTopicLabel` in `packages/shared`, Card DTO gains `servedLang`/`isTranslated`/`compactLangs` (the last always `[]` until phase 9); (2) user preference — `Users.language`, `PUT /v1/me/language`, `GET /v1/me` returns it, an `X-Device-Language` header (mirroring `X-Device-Id`) lets `UsersRepo.touch`'s `if_not_exists` default a brand-new user's language from the device locale on first sight, settings/onboarding both gained a language-picker section; (3) feed serving (D21) — a shared `core/i18n/selectCardVariant.ts` picks `post.i18n[lang]` when present else English, reused by both `toCard.ts` and the digest builder so the fallback exists in exactly one place; (4) translate stage (D22) — `TranslateQueue`+DLQ (SQS ESM `scalingConfig.maximumConcurrency: 2`, a per-event-source-mapping cap distinct from Lambda reserved concurrency, so it doesn't hit the D16 account-quota wall), the feed read path conditionally stamps `i18nPending[lang]` (via a new `core/i18n/needsTranslation.ts`, self-healing after a 10-minute staleness window) and enqueues a translate job; a `translateArticle`/`translateCard` pair mirrors `transformArticle`/`generateCard`'s exact core/handler split, with self-critique-in-call translation and one repair-retry; (5) caps & quotas — `CountersRepo.incrementIfUnderCap` generalized to take a full `counterId` (was hardcoded to `transforms#<date>`) so `translations#<date>` (default 100/day) and a new per-source `transforms#<sourceId>#<date>` quota (default 30/day, `Sources.dailyQuota` override, checked in `transform.ts` alongside the existing global cap) could both be added without duplicating the class; Hugging Face's blog feed was confirmed live to have no distinct official-only variant, so `dailyQuota: 10` was applied to that preset instead; (6) chrome i18n (D20) — `expo-localization` (Expo-Go-bundled, confirmed via a normal `expo install`) + a hand-written `apps/mobile/src/i18n/strings.ts` typed table (no i18n framework dep) covering feed/history/saved/settings/onboarding copy, wired through a `useStrings()` hook backed by a new `languageStore.ts`; a small `isTranslated` badge on `Card` makes the translation pop-in visible per D22's own risk mitigation; (7) digest guard — `composeDigestMessage` now takes the user's language and serves the translated title via the same `selectCardVariant` fallback. 296 vitest + mobile-jest tests green (up from 245). A real bug surfaced live and was fixed before the second deploy: `language` is a DynamoDB reserved keyword — `UsersRepo.touch`/`updateLanguage` used it unaliased, which passed every unit test (`aws-sdk-client-mock` doesn't validate reserved words) but 500'd live on the very first `GET /v1/me` call; fixed by aliasing `#language` like every other attribute in this repo's DynamoDB calls, confirmed by a clean redeploy and a live `X-Device-Language: ru` → `GET /v1/me` round trip. Live-verified end-to-end on `dev`: a fresh device defaulted to `ru` from its `X-Device-Language` header, `PUT /v1/me/language` switched it to `uk`, a `GET /v1/feed` call correctly stamped `i18nPending` and enqueued `TranslateQueue`, and — since the translate consumer runs on a live schedule — the very next feed fetch already served the real Bedrock-translated `uk` title/summary with `isTranslated: true`; `Counters` showed `translations#<date>` incrementing (3) alongside the pre-existing `transforms#<date>` global cap (120 the day before, confirming the cap already bites); both `TranslateQueue` and its DLQ were confirmed drained to 0 messages; the `TranslateDlqDepthAlarm` exists and is `OK`; the HF source's `dailyQuota` override was hand-verified readable via `SourcesRepo.getById` after a manual `update-item` (the existing dev-stage row predates this deploy, so — per D22's own "no backfill" precedent — it needed the one-time manual touch documented in the phase's own verification steps, not a code gap). What's left: no Android SDK/Xcode in this environment, so the on-device Expo Go pass (language switch flips chrome immediately, translated cards render with the badge) is the maintainer's own step, same as every prior phase. **Update 2026-07-24 (D29):** item (7)'s digest guard is moot — the daily digest feature it served was fully retired the same date.

**Phase 9 (compact reader) code complete and verified live end-to-end on the `dev` stage; only the on-device Expo Go pass remains, for the maintainer.** All 7 menu items: (1) guardrails first (D23) — `Sources.compactEnabled` (default true, per-source kill switch) and a `compacts#<date>` daily cap (default 20/day, `COMPACT_DAILY_CAP` env-tunable) are both checked inside `core/pipeline/contentArticle.ts` before any article load or LLM call, mirroring `transformArticle`/`translateArticle`'s exact deps-injected core/handler split; (2) shared contract — `compactBlockSchema` (`paragraph|heading|list|quote|image`, image blocks reference the response's own `figures[]` by index) + `contentResponseSchema` (discriminated on `available`, always HTTP 200 — a content-level "couldn't prepare" is not an error status, same convention as D22) in `packages/shared`; (3) figure extraction + mirroring — a new `core/pipeline/figureExtraction.ts` (added `cheerio` as a direct dep — no HTML-DOM-query library existed in the repo before this) pulls in-body `<img>`/`<figure>` out of the already-extracted article body, deduped against the lead image and the D24 generic-image denylist, capped at 5; `ImageStore.putImage` gained an optional key-suffix parameter so multiple figures per post get distinct CDN keys; (4) compact generation core — `core/llm/compactArticle.ts` (single-pass compress+translate, self-critique-in-call for non-English targets, one repair-retry, golden fixtures) mirrors `translateCard.ts` exactly; (5) content Lambda (D23) — `GET /v1/posts/{postId}/content?lang=` (30s timeout): checks the S3 cache first (near-instant hit), else archive-first article load (one live-fetch fallback, reusing `transformArticle`'s exported `isAllowedByRobots`) → figure mirroring → LLM → writes `content/<postId>/<lang>.json` + `Posts.compactLangs`; a new `Content` S3 bucket is routed at `/content` on the existing `ImagesRouter` (confirmed via `.sst/platform`'s router source that path-prefix matching picks the longest prefix, so `/content` correctly beats the existing `/` image route); (6) reader screen — `/post/[id]` (block renderer, language toggle between the user's language and English, "Read original"/share using the original URL, a graceful "couldn't prepare" fallback for genuine errors); (7) card rewiring (Q17) — `Card.tsx`'s tap now always pushes to the reader instead of opening the browser directly; the reader itself auto-opens the in-app browser and pops the route the instant the content endpoint reports `available:false` (kill switch, over cap, or any content-level generation failure), which is what makes a disabled/over-cap source's cards "route straight to the browser" without needing a new per-source field on the Card DTO. 293 vitest + 37 mobile-jest tests green. No real bugs surfaced this time — deployed clean on the first `sst deploy --stage dev`. Live-verified end-to-end: a real uncached Phys.org post generated a full compact article (5 headings/paragraphs/quotes/lists + 5 mirrored figures referencing the right indices) in ~11.5s, well inside the "≤15s typical" bar; the mirrored figures and the cached JSON both resolve directly via CloudFront (`/images/*` and `/content/*`); a second request for the same post+lang returned from the S3 cache in ~0.2s; flipping a real source's `compactEnabled` to `false` (reverted after) made its posts return `available:false` instantly with zero Bedrock/mirror work; hand-setting today's `compacts#<date>` counter to the cap (reverted after) made a fresh post return `available:false, reason: "over daily compact cap"` instantly; `Posts.compactLangs` correctly picked up `["en"]` after the successful generation; the content Lambda's CloudWatch logs show zero errors across the whole test run. What's left: no Android SDK/Xcode in this environment, so the on-device Expo Go pass (tap a card → reader renders with a mirrored figure, language toggle, "Read original", share, and a disabled/over-cap source falling straight through to the browser) is the maintainer's own step, same as every prior phase. **Update this section whenever a phase lands.**

**Phase 10 (extension polish & cost truth) — the three codeable items are done; the two time-gated acceptance criteria (cost review, two-week soak) cannot be completed yet since phases 8/9 only deployed 2026-07-22, one day before this phase started.** (1) Digest localization end-to-end — the feed path's on-demand translation enqueue (`needsTranslation` check → `setI18nPending` → `TranslateQueue`) was pulled out of `functions/src/api/feed.ts` into a shared `functions/src/translate/enqueueTranslations.ts` and the digest handler (`functions/src/pipeline/digest.ts`) now calls it for its top-5 unread before composing the push, so a digest recipient's untranslated top stories get queued for translation the same on-demand way as the feed, under the existing `translations#<date>` cap; `composeDigestMessage` already picked the translated variant via `selectCardVariant` when present, so this closes the gap where nothing had ever triggered the translation for digest-only posts. (2) Bad-translation feedback — a shared `apps/mobile/src/utils/feedback.ts` (`FEEDBACK_EMAIL` + `translationFeedbackMailto(postId, lang)`) is wired to long-press: on `Card.tsx` when `card.isTranslated`, and on the reader's header row when the currently-viewed variant (`data.lang`) isn't English; both open a mailto prefilled with the postId + lang so a report is traceable to the exact translation without the user typing anything — this is the data the implementation plan's phase 10 said would decide whether the deferred verify pass (DESIGN §12) gets built, so it needs to actually accumulate real reports before that call can be made. `settings.tsx`'s existing feedback row now shares the same `FEEDBACK_EMAIL` constant instead of a duplicated literal. (3) Runbook — `docs/RUNBOOK.md` didn't exist (phase 6 hasn't run), so it was created fresh with the three phase-10-specified sections (stuck `TranslateQueue` DLQ diagnosis/redrive, a compact-generation failure spike — which needs its own diagnosis path since D23's degrade convention means it's HTTP 200 `available:false`, invisible to `Api5xxAlarm` — and a cap-tuning playbook for all four LLM daily caps), plus a D23 kill-switch removal procedure that had been called for in phase 9 but never written down; it explicitly flags phase 6's broader scope (generic broken-source/Bedrock-outage/cost-spike triage) as not yet covered. (4) UX debt pass — reviewed bottom-bar spacing, reader typography, and stub palette; found nothing concretely broken (the bar's 8/40/8px math is intentional, not a bug) — the only real gap left across phases 7-9 is the on-device visual pass, which isn't code-fixable. 293 vitest + 37 mobile-jest tests green (unchanged — this phase's items were reuse/wiring/docs, no new branches needing dedicated tests beyond existing `feed`/`digest` handler conventions, which are thin and intentionally untested the same way both already were). Not attempted, and can't be yet: the Cost Explorer review "one week after phases 8-9 are live" and the "two weeks of daily use, no manual intervention" bar — both need real elapsed time and real usage data this environment can't fabricate. **Update 2026-07-24 (D29):** item (1)'s digest-localization wiring, and the digest feature itself, were fully retired the same date — see D29.

**Phase 11 (UI component library adoption, D26) code complete; only the on-device Expo Go pass remains, for the maintainer.** `react-native-paper` (pure JS, no native linking) is now the app's shared component library, wrapped in `PaperProvider` at `app/_layout.tsx` with full stock `MD3LightTheme`/`MD3DarkTheme` switched by the same `useColorScheme()` expo-router's own `ThemeProvider` already used — no custom theme seed, per D26. Full sweep, every ad hoc `Pressable`+`Text` in `apps/mobile/src` is gone (confirmed via a repo-wide grep, zero remain): `Card.tsx`'s `topicChip`/`translatedBadge`/`debugBadge` custom Views became Paper `Chip`s (colors kept as per-instance `style`/`textStyle` overrides — this is component-level styling for the scheme-independent photo overlay, same as the pre-existing `Colors.overlay` tokens, not a seeded app theme); its main tap/long-press content wrapper became `TouchableRipple` (needed a wrapping `<>...</>` around the multiple children — `TouchableRipple` calls `React.Children.only` internally, so Biome's `noUselessFragments` is suppressed there with a `biome-ignore` explaining why); `BookmarkButton`/`BottomActionBar`'s emoji-icon buttons became Paper `IconButton`s, with `Link href="..." asChild` wrapping the nav icons (confirmed `expo-router`'s `Link` supports `asChild` via `BaseExpoRouterLink`); `settings.tsx`/`onboarding.tsx`'s topic/language picker rows became Paper `List.Item` (+ `List.Icon` for the selected checkmark); `onboarding.tsx`'s CTA and the reader's (`post/[id].tsx`) "Read original"/language-toggle became Paper `Button`; `history.tsx`/`saved.tsx`'s rows became `List.Item` (saved's per-row delete stayed a sibling `IconButton` next to `List.Item` rather than nested inside its `right` slot, since `List.Item` wraps its whole content — including `right` — in one `TouchableRipple`, and nesting touchables there felt like an unnecessary risk for no benefit over keeping them side-by-side). Now-dead tokens/styles were removed after confirming zero remaining references: `Radius.pill`/`Radius.sm` (theme.ts), and the old `ctaButton`/`shareButton`/`removeButton`/emoji-icon style objects across the touched files. **Peer-dep surprise D26 didn't anticipate:** `@expo/vector-icons` wasn't in the repo at all — without it, Paper's `Icon`/`IconButton`/`Chip` icon rendering silently falls back to nothing (confirmed by reading `MaterialCommunityIcon.tsx`'s `loadIconModule()` fallback chain). Added it via `pnpm --filter mobile add @expo/vector-icons`; `expo install --check` confirmed the resolved `^15.1.1` is already the expected version for this repo's Expo SDK 57, and it's pure JS backed by `expo-font` (already a dependency), so it's Expo-Go-safe. 287 vitest + 37 mobile-jest tests green, no regressions (test counts unchanged — this phase swapped underlying components with no behavior change, and the only files with existing snapshot tests, `LoadingScreen`/`ImageStub`, weren't touched). What's left: no Android SDK/Xcode in this environment (confirmed: `xcode-select -p` only resolves the Command Line Tools, no full Xcode.app; no `$ANDROID_HOME`, no `adb`/`emulator` on `PATH`), so the physical-device Expo Go pass across feed/reader/settings/onboarding/saved/history is the maintainer's own step, same as every prior phase. As a proxy, `expo export --platform android` and `--platform ios` both bundled cleanly (1625 and 1503 modules resolved respectively, `MaterialCommunityIcons.ttf` correctly picked up as a bundled asset) — this rules out missing-module/resolution crashes but is not a substitute for seeing Paper's MD3 components actually render.

**Phase 12 (eager translation pipeline + job-polling reader progress, D27/D31) code complete and verified live end-to-end on the `dev` stage; only the on-device Expo Go pass and the week-later Cost Explorer read remain, for the maintainer.** Task 1 (D31) done first and completely, per the phase's own sequencing: deleted `CountersRepo` (+ its table/test), `Sources.dailyQuota`, and every `checkDailyCap`/`incrementIfUnderCap` call site in `transformArticle`/`translateArticle`/`contentArticle` and their three Lambda handlers; `TransformKind`/`transformKindSchema` narrowed from `'llm'|'excerpt'|'skipped'` to just the two real outcomes; `infra/storage.ts`'s `Counters` table and all `COUNTERS_TABLE_NAME`/cap-env wiring in `infra/pipeline.ts`/`infra/api.ts` removed. Task 2 (D27 eager translation): `transformArticle` gained an unconditional `enqueueTranslations` dep, called for every post (degraded or not) right after `updatePost`, wired in `transform.ts` to `TranslateQueue.enqueuePending` for all non-EN `LANGUAGES`; the feed-path lazy mechanism (`enqueueTranslations.ts`, `needsTranslation.ts`, `Posts.i18nPending` + its `PostsRepo` methods) was deleted entirely — `selectCardVariant`'s EN-fallback needed no changes. Task 3 (D27 job-polling content API): `POST /v1/posts/{id}/content` (cache hit completes a job inline, a miss enqueues to a new `ContentJobQueue`) + `GET /v1/posts/{id}/content/status?jobId=` (polls a new short-TTL `ContentJobs` DynamoDB table via `ContentJobsRepo`); the actual generation logic moved verbatim into a new `contentJob.ts` SQS consumer, which stamps real stage transitions (`fetching`→`extracting`→`translating`→`done`) by wrapping each `ContentDeps` function rather than touching `contentArticle.ts` itself — the `compactEnabled` kill switch stayed exactly where it was. Task 4: the reader (`post/[id].tsx`, already Paper-converted by phase 11 — re-checked, no conflicts) replaced its spinner with a `react-query`-polled `ProgressBar` driven by the real job stage, restarting cleanly on language toggle since `viewLang` is part of the query key. 283 vitest + 37 mobile-jest tests green (down from 287 vitest pre-phase — net effect of deleting `countersRepo.test.ts`/`needsTranslation.test.ts` outweighing the new `contentJobsRepo.test.ts`/`contentJobQueue.test.ts`; mobile-jest count unchanged). **A real bug surfaced live, not caught by tests:** 1,740 pre-D31 `Posts` rows on `dev` still carried `transform: "skipped"`, which the narrowed schema now rejects — any feed page touching one 500'd (`ZodError` in CloudWatch). Per the user's explicit choice (matching this repo's established precedent for stale pre-schema-change rows), all 1,740 were deleted from the `dev` table rather than backfilled or schema-patched; feed pagination confirmed clean afterward. Live-verified end-to-end on `dev`: a real ingest-pipeline execution found no new posts (RSS hadn't changed since the last cycle) so eager translation was proven instead by re-enqueueing an existing post through `TransformQueue` directly — `i18n` picked up `ru`/`uk`/`pl` within ~10s of the transform completing, with no feed read in between; the job-polling API was exercised via curl end-to-end (`POST` → real `"stage":"translating"` mid-flight → `"stage":"done","available":true` with generated blocks in ~15s); the `compactEnabled` kill switch was hand-toggled false on a real source and confirmed to short-circuit a job to `available:false` instantly (reverted after); all six queues (`Transform`/`Translate`/`ContentJob` + their DLQs) were confirmed drained to 0 messages after the run. What's left: no Android SDK/Xcode in this environment, so the on-device Expo Go pass (feed shows no translation pop-in, reader shows real staged progress, language toggle restarts the job) is the maintainer's own step, same as every prior phase — `expo export --platform android` bundled cleanly (1625 modules) as a proxy. The Cost Explorer review of real uncapped spend, one week after this rollout, is the phase's other explicitly time-gated acceptance criterion and cannot be done yet.

**Phase 13 (LLM provider swap to OpenRouter, D32) code complete; deploy-and-verify is blocked on the maintainer setting the secret.** All three LLM pipeline paths (card transform, translation, compact-article) now select their provider through one shared factory instead of constructing a Bedrock client directly: `packages/core/src/llm/openRouterClient.ts` (`createOpenRouterProvider(apiKey, model)`, a plain `fetch` against OpenRouter's OpenAI-compatible chat-completions endpoint, same `complete(prompt): Promise<string>` shape as `bedrockClient.ts`, no new HTTP dependency) and `packages/core/src/llm/providerFactory.ts` (`createConfiguredLlmProvider(env)`, reading `LLM_PROVIDER` — default `openrouter` — to pick OpenRouter or reuse the existing `createBedrockClient`/`createBedrockProvider` for the `bedrock` branch). `transform.ts`, `translate.ts`, and `contentJob.ts` each replaced their `getBedrockProvider` lazy factory with `createConfiguredLlmProvider(process.env)`; no other change to any of the three pipeline paths. `infra/pipeline.ts` gained `const openRouterApiKey = new sst.Secret('OpenRouterApiKey')` plus `OPENROUTER_MODEL_ID` (default `anthropic/claude-haiku-4.5`) and `LLM_PROVIDER` constants, linked/added to the transform, translate, and content-job functions' `environment`; the existing `bedrock:InvokeModel` IAM permission blocks and `BEDROCK_MODEL_ID` wiring were left untouched, so the fallback stays fully live. One implementation-detail deviation worth flagging: the secret's value is passed as a plain `OPENROUTER_API_KEY` environment variable (`openRouterApiKey.value` resolved by Pulumi at deploy time) rather than through SST's `Resource.OpenRouterApiKey.value` runtime-linking API — the latter needs a regenerated `sst-env.d.ts`, which only exists after a live `sst dev`/`deploy` with the secret already set, and triggering that live run was out of scope for this session (would also require setting a real or placeholder secret value, which the task explicitly reserved for the maintainer). This trades a small amount of secrecy (the key would be readable via `lambda:GetFunctionConfiguration`/the AWS console, same exposure surface as every other env var already wired this way in this repo) for a change that type-checks cleanly today; switching to `Resource`-based linking once a first live deploy exists is a clean follow-up, not a design change. 304 vitest + 37 mobile-jest tests green (up from 294 vitest): `openRouterClient.test.ts` (mocked global `fetch` — success, non-OK response, missing/malformed content) and `providerFactory.test.ts` (both provider branches, missing-required-env cases for each). `pnpm lint && pnpm typecheck && pnpm test` all green, including `infra/pipeline.ts` against the real SST platform types (`.sst/platform` already existed in this environment from prior phases' live deploys). **What's left, blocking tasks 6–7 and 3 of the phase's 7 acceptance criteria:** no API key was generated, requested, or stored anywhere in code, tests, or commits, per the task's explicit instruction — the maintainer needs to run `npx sst secret set OpenRouterApiKey <value> --stage dev` (and again `--stage production`) themselves, then `sst deploy --stage dev` and confirm via CloudWatch logs that a real transform/translate/compact call completes via OpenRouter, then temporarily flip `LLM_PROVIDER=bedrock` on `dev`, redeploy, confirm the fallback path still works, and revert. **Update 2026-07-25 (D38):** `OPENROUTER_MODEL_ID`'s default in `infra/pipeline.ts` changed from `anthropic/claude-haiku-4.5` to `google/gemini-3.1-flash-lite` — a pricing comparison against OpenRouter's live catalog showed it ~75%/70% cheaper on input/output at the same current-gen tier, with a larger context window and no expected quality regression on multilingual translation; see D38 for the full comparison and rationale. No other change — same secret, same provider factory, same deploy-and-verify steps still pending the maintainer.

**Phase 15 (eager compact-article generation, D36) code complete and verified live end-to-end on the `dev` stage, with zero maintainer-only steps remaining.** `transformArticle` gained an unconditional `enqueueContentJobs` dep, called right after `enqueueTranslations` (same "every post, degraded or not" independence as D27's translation enqueue) — `transform.ts` wires it to enqueue all 4 `LANGUAGES` to the renamed `ContentQueue`, but only after checking the post's source `compactEnabled` first (a new checkpoint distinct from the existing per-language check the consumer already does, so a disabled source's posts never get queued at all — this repo's dormant `sourcesTable`/`SOURCES_TABLE_NAME` link on the transform Lambda, left over from before D31 removed per-source daily quotas, turned out to be exactly what this needed with no infra change). `packages/core/src/pipeline/contentArticle.ts`'s `ContentInput` gained an optional `mirroredFigures` field and `ContentDeps` a `saveMirroredFigures` dep: when the post already has a mirrored figure list, extraction and mirroring are both skipped entirely and the stored list is reused; when absent, this job extracts + mirrors (unchanged logic) and persists the result via a new `PostsRepo.setMirroredFigures` (plain overwrite, matching `setCompactLangs`'s existing precedent) — two language jobs racing on a brand-new post's first message may both mirror and both write, an accepted narrow-race tradeoff per the design, not guarded against. `ContentJobQueue`→`ContentQueue` (renamed, same batched-send shape as `TranslateQueue` — no more `jobId`), `ContentJobsRepo`/the `ContentJobs` DynamoDB table/`contentStatus.ts` all deleted outright (D31's `CountersRepo` removal precedent); `packages/functions/src/pipeline/contentJob.ts` renamed to `content.ts` and stripped of every job-stage-stamping wrapper; the API collapsed `POST /v1/posts/:id/content` + `GET .../content/status?jobId=` into a single `GET /v1/posts/:id/content?lang=` plain S3 cache read (a cache hit returns blocks/figures immediately, a miss returns a typed `available:false` with a `reason` distinguishing `"compactEnabled is false"` from `"not ready yet"` — no LLM call ever happens on this request path); the mobile reader (`post/[id].tsx`) dropped the job-polling `react-query`/`ProgressBar` pair for one plain `useQuery` fetch, with a Paper `ActivityIndicator` + a single new `reader.loading` i18n string replacing the three now-meaningless `stageFetching`/`stageExtracting`/`stageTranslating` strings across all 4 languages. 326 vitest + 37 mobile-jest tests green (up from 319+37 pre-phase). **A real correctness bug surfaced during live verification, not caught by any unit test:** `setCompactLangs`'s pre-existing read-then-overwrite pattern (read the post's current `compactLangs`, append the new language client-side, `SET` the whole array back) was harmless before this phase — at most one language was ever generated per post at a time — but D36's own eager 4-language fan-out put up to 4 concurrent writers on the same field, and a live re-transform of a real `dev` post reproduced the loss immediately (`ru`'s S3 content was written but its language silently vanished from the DynamoDB array, clobbered by a concurrent writer's stale overwrite); fixed by replacing it with `PostsRepo.appendCompactLang`, a single atomic `UpdateItem` using `list_append(if_not_exists(...))` guarded by a `NOT contains(...)` condition, so concurrent per-language writers can never clobber each other and a duplicate append (a re-transformed post) is a harmless no-op instead of a double entry — re-verified live afterward with a fresh post and confirmed all 4 languages landed correctly in `compactLangs`, matching the S3 objects exactly. Live-verified end-to-end on `dev` via direct `TransformQueue` re-enqueues of real existing posts (RSS hadn't changed since the last ingest cycle, same precedent D27/D34 used): a ScienceDaily post generated `en`/`ru`/`uk` compacts while `pl` degraded gracefully on a genuine LLM JSON-parse failure (content-level, logged, no DLQ, no alarm — exactly the designed degrade path); a Hugging Face Blog post generated all 4 languages cleanly, its 5 in-body figures mirrored to CloudFront exactly once (confirmed via `Posts.mirroredFigures`) and reused correctly across languages; `GET /v1/posts/:id/content?lang=en` returned the real cached blocks/figures over HTTP, a nonexistent post correctly 404'd, and hand-toggling a real source's `compactEnabled` to `false` (reverted after) correctly made both the eager enqueue skip entirely (zero content-consumer invocations for that post, confirmed via CloudWatch) and the API return `available:false, reason:"compactEnabled is false"`; all 6 queues (`Transform`/`Translate`/`Content` + their DLQs) were confirmed drained to zero after every test. Unlike every prior phase touching mobile/native code, this phase has no maintainer-only step left outstanding — the reader's plain-fetch behavior was exercised via the same live API calls above, and the UI change itself (dropping a progress bar for a spinner) carries negligible on-device risk.

**Phase 16 (visual identity redesign, "Orbit," D37) code complete and verified via decoded compiled resources; only the real APK build/on-device check remains, blocked by missing Android tooling in this environment.** Replaces D30's violet/magenta "T" lettermark outright with a new concept: navy (`#111A33`) background, amber (`#FF9F1C`) minimal ring-and-dot ("orbit") mark, flat rendering — a stroke-only ring (radius 216, stroke 52 on a 1024 canvas) plus a detached filled dot (radius 50, 34px gap, at -40°), generated via an SVG source rasterized with `sharp` (ephemeral scratchpad install, not a committed dependency, matching D30's own precedent) into the same 6-file set D30 touched: `app.json`'s icon/adaptiveIcon/splash config, all Android adaptive-icon layers (foreground/background/monochrome), the favicon, and `LoadingScreen.tsx`'s background color (its snapshot test regenerated). **The actual point of this phase — fixing the real bug D30 left behind:** D30's asset/config/JS changes were correct but the committed bare `android/` project (D18) was never regenerated via `expo prebuild` to match, so every real APK build since kept baking in the original default Expo template icon/splash; this was confirmed live before any fix by decoding the compiled `mipmap-*/ic_launcher*.webp`/`drawable-*/splashscreen_logo.png`/`values/colors.xml` files directly (still the `#208AEF` Expo chevron). Fixed via a surgical resource sync rather than a blind `expo prebuild --clean`: a temporary `git worktree` (checked out from a `git stash create` commit, so the workspace's pnpm-linked `node_modules` resolved correctly) ran `expo prebuild --platform android` into a scratch copy; a full `diff -rq` against the committed `android/` tree confirmed the fresh prebuild's default `build.gradle` lacked D18's `keystoreProperties`-conditional release `signingConfig` entirely (proving why `--clean` would have destroyed it), and surfaced three more pre-existing, out-of-scope sync gaps left untouched — `AndroidManifest.xml` (missing an `expo-localization`-added `configChanges` token), `values/strings.xml` (`expo_runtime_version` stuck at `0.0.1`), and `gradle.properties` (missing phase-14-era CI build-perf tuning). Only `mipmap-*/ic_launcher*.webp` (all 5 densities), `drawable-*/splashscreen_logo.png` (all 5 densities), and `values/colors.xml`'s two color values were copied over; `build.gradle` confirmed byte-identical via `sha256sum` before and after. 294 vitest + 37 mobile-jest tests green (including the regenerated `LoadingScreen` snapshot). Verified by decoding the copied compiled files directly, not by re-reading `app.json`/source-asset content (D30's own verification gap): `ic_launcher.webp`, `ic_launcher_foreground.webp`, and `splashscreen_logo.png` all now show the Orbit mark on navy, and `colors.xml`'s `splashscreen_background` reads `#111A33`. What's left, and explicitly blocked rather than deferred: a real APK build and on-device/emulator check — this environment has no Android SDK, no `adb`/`gradle`, and no Java runtime at all (`ANDROID_HOME`/`ANDROID_SDK_ROOT` unset, no SDK at the default macOS path, `java -version` can't locate a runtime), so this is the maintainer's own step, same constraint every prior phase (7, 10, 11, 12) hit — but unlike D30, every other acceptance criterion for this phase was proven against the actual compiled resources, not just source/config. **Update this section whenever a phase lands.**

**Phase 14 (CI/CD hardening, D33–D35) code complete and verified, including one real E2E run against the `dev` stage; only adding the `AWS_E2E_ROLE_ARN` GitHub secret remains, for the maintainer.** Four independent pieces, each implemented and verified on its own: (1) parallel CI jobs (D33) — `.github/workflows/ci.yml`'s single `quality` job split into a `setup` job (installs deps once, tars every workspace's `node_modules`, uploads as a build artifact) plus `lint`/`typecheck`/`test`/`schema-check` running in parallel off that same artifact via `actions/download-artifact`; `deploy`'s `needs:` now `[lint, typecheck, test]`; (2) schema snapshot diff (D34 part 1) — `packages/shared/scripts/schemaSnapshot.ts` serializes every exported `*Schema` in `schemas.ts` via zod 4's own `z.toJSONSchema()` to a committed `packages/shared/schema-snapshot.json`, and a recursive structural diff (`diffSnapshots`, unit-tested) flags a removed field, a required→optional transition, a changed `type`, or a removed enum value — new fields/enum values/schemas pass silently; a `schema-check` CI job runs it in `check` mode; regenerating and committing the snapshot *is* the "acknowledged override" the design called for (visible as a plain JSON diff to reviewers), so no separate override mechanism was built; (3) dev-stage E2E workflow (D34 part 2) — a new `packages/e2e` workspace package with two suites (`backend-pipeline`: starts a real `IngestPipeline` execution, asserts every `Sources` row gets a fresh `lastFetchAt`, waits for `TransformQueue`/`TranslateQueue` to drain; `api-contract`: hits `/v1/topics`, `/v1/me`, `/v1/feed`, `/v1/history`, `/v1/bookmarks` as a fresh device and parses every response through the real `@techtok/shared` schemas), both discovering the dev stage's real resource identifiers via `ResourceGroupsTaggingAPI` tag search on the existing `app: techtok-dev` tag (D17) rather than guessing SST/Pulumi-generated physical names; `.github/workflows/e2e.yml` (`workflow_dispatch` + daily schedule, never a PR trigger) runs both via a new least-privilege `techtok-gha-e2e` IAM role (OIDC, read/invoke-only on `techtok-dev-*` resources — no write/deploy access, unlike the admin-privileged deploy role); (4) mobile semver automation (D35) — `scripts/bumpMobileVersion.ts` (conventional-commit classification + semver bump, both pure and unit-tested) plus `.github/workflows/mobile-version.yml`, triggered on the same mobile-relevant path filter `mobile-build.yml` uses, which runs the script then commits+tags+pushes only if the three version files actually changed. 319 vitest + 37 mobile-jest tests green (up from 294+37 pre-phase). A real pre-existing bug surfaced while building the E2E suite, unrelated to any of the four pieces themselves: `he@1.2.0`'s CJS export (`module.exports = he`, a variable, not a static object literal) can't be statically analyzed by Node's `cjs-module-lexer`, so `import { decode } from 'he'` — silently papered over by every bundler this repo already uses (esbuild for Lambda, Vite for vitest) — throws under a real Node ESM loader (tsx, needed to run the E2E scripts outside a bundle); fixed in `core/ingest/htmlText.ts` with a default-import workaround, harmless everywhere else. Both E2E suites were run live end-to-end against the real `dev` stage during implementation using local AWS credentials (not yet via the Actions runner itself): the backend-pipeline suite's `IngestPipeline` execution succeeded, all 11 enabled sources picked up a fresh `lastFetchAt`, and both queues drained to zero; the API-contract suite's five endpoints all parsed cleanly against `packages/shared`. The pre-existing three-way mobile version drift (`app.json` 0.0.1 / `package.json` 0.0.0 / `build.gradle` 0.0.1+versionCode 1) was reconciled for real as this decision's first run — `app.json`/`package.json`/`build.gradle` all now read `0.0.1`/versionCode 2, tagged `mobile-v0.0.1` at the commit that landed this phase. What's left: the `techtok-gha-e2e` IAM role exists in AWS (created live, permissions verified via `iam simulate-principal-policy` against real resource ARNs), but only the maintainer can add `AWS_E2E_ROLE_ARN` as a GitHub Actions secret (the same maintainer-only step `AWS_DEPLOY_ROLE_ARN` needed in phase 2) — until then `e2e.yml`'s own Actions runs will fail on the credentials step even though both suites are proven correct against real AWS. **Update 2026-07-25 (D38):** `.github/workflows/ci.yml` was restructured into one linear main-branch pipeline — quality jobs → `deploy-dev` → `e2e` → `deploy-production` → `mobile-build`. Each of those four stages is its own reusable workflow file (`deploy-dev.yml`, `e2e.yml`, `deploy-production.yml`, `mobile-build.yml`), pulled into `ci.yml` via `uses:`/`secrets: inherit` — the same `workflow_call` pattern `e2e.yml` already used — rather than inlined as job steps; each file keeps a standalone `workflow_dispatch` trigger too. `mobile-build.yml` (replacing the old top-level `mobile-build.yml` job) takes the real production API URL as a required `api_url` workflow-call input, wired from `deploy-production`'s own declared output, and injects it as `EXPO_PUBLIC_API_URL` at build time instead of a hardcoded `eas.json` placeholder. `.github/workflows/mobile-version.yml` (D35's version-bump automation) was deleted outright as no longer fitting that shape; `scripts/bumpMobileVersion.ts` still exists for manual use. `deploy-dev` authenticates via a brand-new, purpose-built `techtok-gha-dev-deploy` IAM role — not a reuse of the admin-privileged `AWS_DEPLOY_ROLE_ARN` — created and live-tested this same session: an inline policy scoped to `techtok-dev-*`-named/`stage: dev`-tagged resources across every AWS service `infra/*.ts` touches, plus a hard explicit-`Deny` backstop on anything tagged `production` or belonging to this account's other SST app (`uweather`); a handful of services that genuinely can't be scoped that tightly (EventBridge Scheduler, AWS Budgets, API Gateway v2 sub-resources, several CloudFront sub-resource types) are accepted broad exceptions, documented in D38. Verified live via `sts assume-role` (temporary local-testing trust, removed immediately after) running a real `npx sst deploy --stage dev` with zero `AccessDenied` errors — that run found no diff, so only the read/diff side was exercised, not every mutate action. **What's left:** the maintainer needs to add `AWS_DEV_DEPLOY_ROLE_ARN` (`arn:aws:iam::592147423332:role/techtok-gha-dev-deploy`) as a GitHub Actions secret before `deploy-dev` can run in CI, on top of the still-outstanding `AWS_E2E_ROLE_ARN` from phase 14. **Update this section whenever a phase lands.**

## Commands (canonical root scripts)

```
pnpm install
pnpm lint        # Biome — this repo has NO ESLint/Prettier (D7)
pnpm typecheck   # tsc --noEmit across workspaces + sst.config.ts/infra (the latter only after a first `pnpm dev`)
pnpm test        # vitest (shared/core/functions) + jest (mobile)
pnpm dev         # sst dev --stage dev — live Lambda on your personal "dev" stage
pnpm --filter mobile start   # Expo dev server
```

Definition of done for any change: lint + typecheck + tests green, then exercised on the dev stage / a device. Verify by running the commands, not by reading the code.

## Layout (planned)

- `packages/shared` — zod v4 contracts + topic taxonomy; imported by server **and** app; no runtime deps beyond zod
- `packages/core` — all business logic (RSS mapping, URL canonicalization, feed merge, DynamoDB repos, LLM client)
- `packages/functions` — thin Lambda handlers only: parse input → call core → serialize
- `apps/mobile` — Expo app (expo-router)
- `infra/` — SST components imported by `sst.config.ts`

## Hard rules

- TypeScript strict, Node 22, pnpm workspaces — never npm/yarn commands.
- Handlers stay thin: if logic can't be unit-tested without AWS, it belongs in `core`, not `functions`.
- Every API request/response shape is a zod schema in `packages/shared`; server parses inputs, app parses responses.
- Tests never call live AWS or Bedrock: `aws-sdk-client-mock` + recorded LLM golden fixtures. CI must run with no AWS credentials. *(Exception, DESIGN §2 D34, amended D38: `.github/workflows/e2e.yml` authenticates via AWS OIDC to exercise the real `dev` stage — triggered by schedule/`workflow_dispatch`, and as a required step of `ci.yml`'s main-branch pipeline (between the dev and production deploys). Never triggered by a PR. Every PR-triggered job stays credential-free.)*
- Pipeline failure split (DESIGN §7.2): content-level failures **degrade** (excerpt card, feed never starves); infra-level failures **throw** → SQS retry → DLQ → alarm. Never invert this.
- LLM calls go only through the three defined pipeline paths — card transform, translate (eager as of D27), compact-article (eager for all 4 languages as of D36, phase 15). No ad-hoc LLM-provider calls anywhere else (OpenRouter primary / Bedrock dormant fallback per DESIGN §2 D32, phase 13). Daily caps/per-source quotas on these paths were removed for simplicity (DESIGN §2 D31, phase 12) — the $10/mo AWS Budget alarm (D11) is a monitoring-only signal now, not an enforced ceiling, and doesn't see OpenRouter spend at all (D32 — separate bill, no AWS-side visibility). (Reserved concurrency 2 on the transform Lambda remains deferred pending an AWS account quota fix — see DESIGN §2 D16.)
- Feed access follows the key design in DESIGN §6 (primaryTopic GSI, read-markers via BatchGet). No table scans or filter-expression shortcuts on `Posts`.
- Keep React Native code cross-platform (D12): no Android-only APIs without a `Platform` guard.
- Conventional commits: `feat:` / `fix:` / `docs:` / `chore:` / `test:` / `refactor:`. *(These types are also the input to the mobile app's automated semver bump — DESIGN §2 D35.)*
- Every request/response shape change to `packages/shared`'s zod schemas gets checked against the committed schema snapshot before merge (DESIGN §2 D34) — a removed field, narrowed/changed type, or removed enum value fails CI unless the snapshot is regenerated deliberately.

## AWS

- Region `eu-central-1`. Stages: personal dev stage, named `dev` (`sst dev` / `sst deploy --stage dev`, formerly the OS-username default `andrey`, briefly `stage` — see D17) and `production` (deployed by CI only — don't `sst deploy --stage production` from a laptop). All resources carry default tags `app: techtok-dev|techtok-production` + `stage: <stage name>` for Cost Explorer grouping (D17).
- Never run `sst remove` (denied in settings.json); it destroys deployed stacks.
- Budget ceiling is **$10/mo** (D11). Anything cost-bearing — schedule rates, LLM volume/caps, log retention, new always-on resources — gets checked against DESIGN §10 first.

## Git & PR Workflow

- The `gh` account authenticated in this environment is a non-collaborator with read-only access to this repo (`viewerPermission: READ`) — `gh pr create` will fail every time. Don't attempt it.
- After a change is ready (quality gates green, commit pushed): push the branch, then print a ready-to-click compare link — `https://github.com/tormozz48/techtok/compare/main...<branch>?expand=1` — plus a suggested PR title and body. Let the maintainer open the PR from that link.
- If repo permissions change (confirm via `gh repo view tormozz48/techtok --json viewerPermission`), this restriction can be dropped.

## Quality Gates

Before every commit: `pnpm lint`, then `pnpm typecheck`, then `pnpm test` — all must exit 0. For any `apps/mobile` change, also run a Metro bundle check (`pnpm --filter mobile exec expo export --platform android`) as a cheap proxy for a real device pass. Use `/check` to run this loop and fix failures until green; don't commit on a partial pass.

## Schema & Data Migrations

- Never narrow or otherwise change a `packages/shared` zod schema (or a DynamoDB item shape) without first counting existing rows in every live stage that would violate the new shape, and writing an explicit migration or cleanup plan for them. (D31's `TransformKind` narrowing shipped without this check and 500'd `GET /v1/feed` on 1,740 pre-existing `dev` rows.)
- Watch for DynamoDB reserved keywords (e.g. `language`, `status`, `name`) in any `UpdateExpression`/`ProjectionExpression` — always alias them via `ExpressionAttributeNames`. `aws-sdk-client-mock` does not catch this; it only surfaces live.

## Claude config in this repo

- `.claude/settings.json` — permission allowlist for the common loop + a PostToolUse hook that auto-formats edited files with Biome (no-ops until Biome is installed in Phase 0).
- `/check` — run all quality gates and fix until green
- `/phase` — report progress against the implementation plan, propose next increment
- `/log-decision` — append a decision to the DESIGN.md decision log
- `/ship` — run `/check`, commit, push, and print a PR compare link (gh pr create is not usable here — read-only account)
