# Copy to .env and point at your `sst dev` (or deployed) API's URL.
# Shown in the `sst dev`/`sst deploy` output as the "api" endpoint.
EXPO_PUBLIC_API_URL=https://your-api-id.execute-api.eu-central-1.amazonaws.com
